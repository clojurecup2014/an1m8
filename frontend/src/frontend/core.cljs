(ns frontend.core
	(:require [frontend.dom :as dom]
            [frontend.anim :as a]))

;
;
; Junta Power!
;
;


(enable-console-print!) ; does not work in ie :)

(println "Junta Power!")

(a/test-core-async)
